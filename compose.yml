services:
  pgadmin_db:
    image: postgres:17
    container_name: pgadmin_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${PGADMIN_DATABASE_PASSWORD}
    command:
      - postgres
      - -c
      - logging_collector=on
      - -c
      - log_directory=/var/log/postgresql
      - -c
      - log_filename=postgresql-%Y-%m-%d.log
      - -c
      - log_file_mode=0644
    volumes:
      - ./volumes/pgadmin_db/data:/var/lib/postgresql/data:z
      - ./volumes/logs/pgadmin_db:/var/log/postgresql:z
      - ./config/pgadmin_db/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - nocodenation_all_in_wonder_network

  postgres:
    image: pgvector/pgvector:pg17
    container_name: postgres
    restart: unless-stopped
    user: "0:0"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: api_user
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    command:
      - postgres
      - -c
      - logging_collector=on
      - -c
      - log_directory=/var/log/postgresql
      - -c
      - log_filename=postgresql-%Y-%m-%d.log
      - -c
      - log_file_mode=0644
    volumes:
      - ./volumes/postgres/data:/var/lib/postgresql/data:z
      - ./volumes/logs/postgres:/var/log/postgresql:z
      - ./config/postgres/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - nocodenation_all_in_wonder_network

  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: unless-stopped
    user: "0:0"
    volumes:
      - ./volumes/pgadmin:/var/lib/pgadmin:z
      - ./volumes/logs/pgadmin:/var/log/pgadmin:z
      - ./config/pgadmin/config_distro.py:/pgadmin4/config_distro.py
      - ./config/pgadmin/config_local.py:/pgadmin4/config_local.py
      - ./config/pgadmin/webserver.py:/pgadmin4/pgadmin/authenticate/webserver.py
      - ./config/pgadmin/servers.json:/pgadmin4/servers.json
      - ./config/pgadmin/pgpass:/pgadmin4/pgpass
    depends_on:
        pgadmin_db:
          condition: service_healthy
        postgres:
          condition: service_healthy
#    ports:
#      - "8100:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      PGADMIN_SERVER_JSON_FILE: /pgadmin4/servers.json
      PGADMIN_CONFIG_WTF_CSRF_ENABLED: "False"
      PGADMIN_CONFIG_ENHANCED_COOKIE_PROTECTION: "False"
      PGADMIN_PASSFILE_PATH: /pgadmin4/pgpass
    networks:
      - nocodenation_all_in_wonder_network

  postgrest_app:
    image: postgrest/postgrest
    container_name: postgrest_app
    restart: unless-stopped
    environment:
      PGRST_DB_URI: postgres://api_user:${DATABASE_PASSWORD}@postgres:5432/postgres
      PGRST_DB_SCHEMA: public
      PGRST_DB_ANON_ROLE: api_anon
      PGRST_OPENAPI_SERVER_PROXY_URI: http://postgrest.localhost:${SYSTEM_HTTP_PORT:-8888}
      PGRST_JWT_SECRET: ${POSTGREST_JWT_SECTET}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - nocodenation_all_in_wonder_network

  proxy:
    image: nginx:latest
    container_name: proxy
    restart: unless-stopped
    ports:
      - "${SYSTEM_HTTP_PORT:-8888}:${SYSTEM_HTTP_PORT:-8888}"
      - "${SYSTEM_HTTPS_PORT:-8833}:${SYSTEM_HTTPS_PORT:-8833}"
    volumes:
      - ./config/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./config/openproject/theme:/usr/share/nginx/custom-theme:ro
      - ./volumes/logs/proxy:/var/log/nginx:z
      - ./volumes/nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - postgrest_app
      - pgadmin
      - openproject-web
      - swagger
      - opencode
      - bun_runner
      - openclaw-gateway
      - hermes
    networks:
      nocodenation_all_in_wonder_network:
        aliases:
          - pgadmin.localhost
          - postgrest.localhost
          - swagger.localhost
          - opencode.localhost
          - openclaw.localhost
          - bridge.openclaw.localhost
          - msteams.openclaw.localhost
          - app.localhost
          - openproject.localhost
          - nextcloud.localhost
          - nifi.localhost
          - 8900.nifi.localhost
          - 8901.nifi.localhost
          - 8902.nifi.localhost
          - 8903.nifi.localhost
          - 8904.nifi.localhost
          - 8905.nifi.localhost
          - 8906.nifi.localhost
          - 8907.nifi.localhost
          - 8908.nifi.localhost
          - 8909.nifi.localhost
          - 8910.nifi.localhost
          - 8911.nifi.localhost
          - 8912.nifi.localhost
          - 8913.nifi.localhost
          - 8914.nifi.localhost
          - 8915.nifi.localhost
          - 8916.nifi.localhost
          - 8917.nifi.localhost
          - 8918.nifi.localhost
          - 8919.nifi.localhost
          - 8920.nifi.localhost
          - 8921.nifi.localhost
          - 8922.nifi.localhost
          - 8923.nifi.localhost
          - 8924.nifi.localhost
          - 8925.nifi.localhost
          - 8926.nifi.localhost
          - 8927.nifi.localhost
          - 8928.nifi.localhost
          - 8929.nifi.localhost
          - 8930.nifi.localhost
          - 8931.nifi.localhost
          - 8932.nifi.localhost
          - 8933.nifi.localhost
          - 8934.nifi.localhost
          - 8935.nifi.localhost
          - 8936.nifi.localhost
          - 8937.nifi.localhost
          - 8938.nifi.localhost
          - 8939.nifi.localhost
          - 8940.nifi.localhost
          - 8941.nifi.localhost
          - 8942.nifi.localhost
          - 8943.nifi.localhost
          - 8944.nifi.localhost
          - 8945.nifi.localhost
          - 8946.nifi.localhost
          - 8947.nifi.localhost
          - 8948.nifi.localhost
          - 8949.nifi.localhost
          - 8950.nifi.localhost
          - 8951.nifi.localhost
          - 8952.nifi.localhost
          - 8953.nifi.localhost
          - 8954.nifi.localhost
          - 8955.nifi.localhost
          - 8956.nifi.localhost
          - 8957.nifi.localhost
          - 8958.nifi.localhost
          - 8959.nifi.localhost
          - 8960.nifi.localhost
          - 8961.nifi.localhost
          - 8962.nifi.localhost
          - 8963.nifi.localhost
          - 8964.nifi.localhost
          - 8965.nifi.localhost
          - 8966.nifi.localhost
          - 8967.nifi.localhost
          - 8968.nifi.localhost
          - 8969.nifi.localhost
          - 8970.nifi.localhost
          - 8971.nifi.localhost
          - 8972.nifi.localhost
          - 8973.nifi.localhost
          - 8974.nifi.localhost
          - 8975.nifi.localhost
          - 8976.nifi.localhost
          - 8977.nifi.localhost
          - 8978.nifi.localhost
          - 8979.nifi.localhost
          - 8980.nifi.localhost
          - 8981.nifi.localhost
          - 8982.nifi.localhost
          - 8983.nifi.localhost
          - 8984.nifi.localhost
          - 8985.nifi.localhost
          - 8986.nifi.localhost
          - 8987.nifi.localhost
          - 8988.nifi.localhost
          - 8989.nifi.localhost
          - 8990.nifi.localhost
          - 8991.nifi.localhost
          - 8992.nifi.localhost
          - 8993.nifi.localhost
          - 8994.nifi.localhost
          - 8995.nifi.localhost
          - 8996.nifi.localhost
          - 8997.nifi.localhost
          - 8998.nifi.localhost
          - 8999.nifi.localhost
          - hermes.localhost
          - api.hermes.localhost
          - webhooks.hermes.localhost

  swagger:
    image: swaggerapi/swagger-ui
    container_name: swagger
    restart: unless-stopped
    environment:
      API_URL: http://postgrest.localhost:${SYSTEM_HTTP_PORT:-8888}/
    volumes:
      - ./volumes/logs/swagger:/var/log/nginx:z
    networks:
      - nocodenation_all_in_wonder_network

  opencode:
    image: all-in-wonder/opencode:latest
    container_name: opencode
    restart: unless-stopped
    extra_hosts:
      - "dgx_spark:${OPENCODE_OLLAMA_HOST_IP:-127.0.0.1}"
      - "embedding_host:${OPENCODE_EMBEDDING_HOST_IP:-127.0.0.1}"
    environment:
      OPENCODE_SERVER_PORT: "4096"
      OPENCODE_MODEL: ${OPENCODE_MODEL:-}
      OPENCODE_USERNAME: ${OPENCODE_USERNAME:-}
      OPENCODE_PASSWORD: ${OPENCODE_PASSWORD:-}
      OPENCODE_ANTHROPIC_KEY: ${OPENCODE_ANTHROPIC_KEY:-}
      OPENCODE_OPENAI_KEY: ${OPENCODE_OPENAI_KEY:-}
      OPENCODE_OPENROUTER_KEY: ${OPENCODE_OPENROUTER_KEY:-}
      OPENCODE_OLLAMA_HOST: ${OPENCODE_OLLAMA_HOST:-}
      OPENCODE_EMBEDDING_HOST: ${OPENCODE_EMBEDDING_HOST:-}
      OPENCODE_EMBEDDING_MODEL: ${OPENCODE_EMBEDDING_MODEL:-}
      OPENCODE_TIMEOUT: ${OPENCODE_TIMEOUT:-600000}
      OPENCODE_CHUNK_TIMEOUT: ${OPENCODE_CHUNK_TIMEOUT:-120000}
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-}
      POSTGREST_API_KEY: ${API_KEY:-}
      NIFI_USERNAME: ${NIFI_USERNAME:-}
      NIFI_PASSWORD: ${NIFI_PASSWORD:-}
      SYSTEM_HTTP_PORT: ${SYSTEM_HTTP_PORT:-8888}
      SYSTEM_HTTPS_PORT: ${SYSTEM_HTTPS_PORT:-8833}
      NIFI_KEYSTORE_PASSWORD: ${NIFI_KEYSTORE_PASSWORD}
    volumes:
      - ./config/agents/instructions.md:/opencode/instructions.md:ro
      - ./config/agents/skills:/root/.config/opencode/skills
      - ./config/opencode/package.json:/root/.config/opencode/package.json
      - ./config/opencode/tools:/root/.config/opencode/tools:ro
      - ./volumes/logs/postgres:/logs/postgres:ro
      - ./volumes/logs/pgadmin_db:/logs/pgadmin_db:ro
      - ./volumes/logs/pgadmin:/logs/pgadmin:ro
      - ./volumes/logs/proxy:/logs/proxy:ro
      - ./volumes/logs/swagger:/logs/swagger:ro
      - ./volumes/logs/bun_runner:/logs/bun_runner:ro
      - ./volumes/app:/app
      - ./volumes/data:/data
    networks:
      - nocodenation_all_in_wonder_network

  bun_runner:
    image: all-in-wonder/bun-runner:latest
    container_name: bun_runner
    restart: unless-stopped
    volumes:
      - ./volumes/app:/app
      - ./volumes/data:/data
      - ./volumes/build_info:/build_info
      - ./volumes/logs/bun_runner:/logs
    environment:
      POSTGREST_API_KEY: ${API_KEY:-}
    healthcheck:
      test: ["CMD-SHELL", "bash -c '> /dev/tcp/127.0.0.1/3000'"]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 5m
    networks:
      - nocodenation_all_in_wonder_network

  openproject-db:
    image: postgres:17
    container_name: openproject-db
    restart: unless-stopped
    volumes:
      - ./volumes/openproject_db:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: ${OPENPROJECT_DATABASE_PASSWORD}
      POSTGRES_DB: openproject
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d openproject" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - nocodenation_all_in_wonder_network

  openproject-cache:
    image: memcached:1.6-alpine
    container_name: openproject-cache
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "echo stats | nc -w1 127.0.0.1 11211 | grep -q uptime" ]
      interval: 30s
      timeout: 5s
      retries: 3
    networks:
      - nocodenation_all_in_wonder_network

  openproject-web:
    image: openproject/openproject:17-slim
    restart: unless-stopped
    container_name: openproject-web
    environment:
      OPENPROJECT_HTTPS: "false"
      OPENPROJECT_HOST__NAME: "openproject.localhost:${SYSTEM_HTTP_PORT:-8888}"
      OPENPROJECT_HSTS: "false"
      RAILS_CACHE_STORE: "memcache"
      OPENPROJECT_CACHE__MEMCACHE__SERVER: "cache:11211"
      DATABASE_URL: "postgres://postgres:${OPENPROJECT_DATABASE_PASSWORD}@openproject-db/openproject?pool=5&encoding=unicode&reconnect=true"
      POSTGRES_STATEMENT_TIMEOUT: "120000"
      OPENPROJECT_LOG__LEVEL: "warn"
      SECRET_KEY_BASE: ${OPENPROJECT_SECRET_KEY_BASE}
      # set to true to enable the email receiving feature. See ./docker/cron for more options
      IMAP_ENABLED: "false"
      OPENPROJECT_AUTH__SOURCE__SSO_HEADER: "X-OpenProject-User"
      OPENPROJECT_AUTH__SOURCE__SSO_SECRET: "ALL_IN_WONDER_USER"
      OPENPROJECT_DISABLE__PASSWORD__LOGIN: "true"
    volumes:
      - ./volumes/openproject:/var/openproject/assets
      - ./config/openproject/auth_source_sso.rb:/app/app/controllers/concerns/auth_source_sso.rb
      - ./config/openproject/proxy_auth_source.rb:/app/app/models/proxy_auth_source.rb
      - ./config/openproject/patch_theme.sh:/patch/patch_theme.sh:ro
    command: ["bash", "-c", "bash /patch/patch_theme.sh; exec ./docker/prod/web"]
    depends_on:
      openproject-db:
        condition: service_healthy
      openproject-cache:
        condition: service_healthy
      openproject-seeder:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://127.0.0.1:8080/health_checks/default",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s
    networks:
      - nocodenation_all_in_wonder_network

  openproject-worker:
    image: openproject/openproject:17-slim
    restart: unless-stopped
    container_name: openproject-worker
    environment:
      OPENPROJECT_HTTPS: "false"
      OPENPROJECT_HOST__NAME: "openproject.localhost:${SYSTEM_HTTP_PORT:-8888}"
      OPENPROJECT_HSTS: "false"
      RAILS_CACHE_STORE: "memcache"
      OPENPROJECT_CACHE__MEMCACHE__SERVER: "cache:11211"
      DATABASE_URL: "postgres://postgres:${OPENPROJECT_DATABASE_PASSWORD}@openproject-db/openproject?pool=5&encoding=unicode&reconnect=true"
      POSTGRES_STATEMENT_TIMEOUT: "120000"
      OPENPROJECT_LOG__LEVEL: "warn"
      SECRET_KEY_BASE: ${OPENPROJECT_SECRET_KEY_BASE}
      # set to true to enable the email receiving feature. See ./docker/cron for more options
      IMAP_ENABLED: "false"
      OPENPROJECT_AUTH__SOURCE__SSO_HEADER: "X-OpenProject-User"
      OPENPROJECT_AUTH__SOURCE__SSO_SECRET: "ALL_IN_WONDER_USER"
      OPENPROJECT_DISABLE__PASSWORD__LOGIN: "true"
    volumes:
      - ./volumes/openproject:/var/openproject/assets
      - ./config/openproject/auth_source_sso.rb:/app/app/controllers/concerns/auth_source_sso.rb
      - ./config/openproject/proxy_auth_source.rb:/app/app/models/proxy_auth_source.rb
    depends_on:
      openproject-db:
        condition: service_healthy
      openproject-cache:
        condition: service_healthy
      openproject-seeder:
        condition: service_completed_successfully
    command: "./docker/prod/worker"
    networks:
      - nocodenation_all_in_wonder_network

  openproject-cron:
    image: openproject/openproject:17-slim
    restart: unless-stopped
    container_name: openproject-cron
    environment:
      OPENPROJECT_HTTPS: "false"
      OPENPROJECT_HOST__NAME: "openproject.localhost:${SYSTEM_HTTP_PORT:-8888}"
      OPENPROJECT_HSTS: "false"
      RAILS_CACHE_STORE: "memcache"
      OPENPROJECT_CACHE__MEMCACHE__SERVER: "cache:11211"
      DATABASE_URL: "postgres://postgres:${OPENPROJECT_DATABASE_PASSWORD}@openproject-db/openproject?pool=5&encoding=unicode&reconnect=true"
      POSTGRES_STATEMENT_TIMEOUT: "120000"
      OPENPROJECT_LOG__LEVEL: "warn"
      SECRET_KEY_BASE: ${OPENPROJECT_SECRET_KEY_BASE}
      # set to true to enable the email receiving feature. See ./docker/cron for more options
      IMAP_ENABLED: "false"
      OPENPROJECT_AUTH__SOURCE__SSO_HEADER: "X-OpenProject-User"
      OPENPROJECT_AUTH__SOURCE__SSO_SECRET: "ALL_IN_WONDER_USER"
      OPENPROJECT_DISABLE__PASSWORD__LOGIN: "true"
    volumes:
      - ./volumes/openproject:/var/openproject/assets
      - ./config/openproject/auth_source_sso.rb:/app/app/controllers/concerns/auth_source_sso.rb
      - ./config/openproject/proxy_auth_source.rb:/app/app/models/proxy_auth_source.rb
    depends_on:
      openproject-db:
        condition: service_healthy
      openproject-cache:
        condition: service_healthy
      openproject-seeder:
        condition: service_completed_successfully
    command: "./docker/prod/cron"
    networks:
      - nocodenation_all_in_wonder_network

  openproject-seeder:
    image: openproject/openproject:17-slim
    restart: on-failure
    container_name: openproject-seeder
    environment:
      OPENPROJECT_HTTPS: "false"
      OPENPROJECT_HOST__NAME: "openproject.localhost:${SYSTEM_HTTP_PORT:-8888}"
      OPENPROJECT_HSTS: "false"
      RAILS_CACHE_STORE: "memcache"
      OPENPROJECT_CACHE__MEMCACHE__SERVER: "cache:11211"
      DATABASE_URL: "postgres://postgres:${OPENPROJECT_DATABASE_PASSWORD}@openproject-db/openproject?pool=5&encoding=unicode&reconnect=true"
      POSTGRES_STATEMENT_TIMEOUT: "120000"
      OPENPROJECT_LOG__LEVEL: "warn"
      SECRET_KEY_BASE: ${OPENPROJECT_SECRET_KEY_BASE}
      # set to true to enable the email receiving feature. See ./docker/cron for more options
      IMAP_ENABLED: "false"
      OPENPROJECT_AUTH__SOURCE__SSO_HEADER: "X-OpenProject-User"
      OPENPROJECT_AUTH__SOURCE__SSO_SECRET: "ALL_IN_WONDER_USER"
      OPENPROJECT_DISABLE__PASSWORD__LOGIN: "true"
    volumes:
      - ./volumes/openproject:/var/openproject/assets
      - ./config/openproject/auth_source_sso.rb:/app/app/controllers/concerns/auth_source_sso.rb
      - ./config/openproject/proxy_auth_source.rb:/app/app/models/proxy_auth_source.rb
    depends_on:
        openproject-db:
          condition: service_healthy
    command: "./docker/prod/seeder"
    networks:
      - nocodenation_all_in_wonder_network

  nextcloud-db:
    image: postgres:17
    container_name: nextcloud-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${NEXTCLOUD_DATABASE_PASSWORD}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d postgres" ]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    volumes:
      - ./volumes/nextcloud_db:/var/lib/postgresql/data
    networks:
      - nocodenation_all_in_wonder_network

  nextcloud-redis:
    image: redis:8
    container_name: nextcloud-redis
    restart: unless-stopped
    volumes:
      - ./volumes/nextcloud_redis/data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"] # Check if Redis responds to PING
      interval: 5s # Check every 5 seconds
      timeout: 3s # Allow 3 seconds for the command to complete
      retries: 5 # Consider unhealthy after 5 consecutive failures
      start_period: 10s
    command: redis-server --requirepass ${NEXTCLOUD_REDIS_PASSWORD}
    networks:
      - nocodenation_all_in_wonder_network

  nextcloud:
    image: nextcloud:stable
    container_name: nextcloud
    restart: unless-stopped
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${NEXTCLOUD_DATABASE_PASSWORD}
      POSTGRES_HOST: nextcloud-db
      NEXTCLOUD_ADMIN_USER: ${PGADMIN_DEFAULT_EMAIL}
      NEXTCLOUD_ADMIN_PASSWORD: ${PGADMIN_DEFAULT_EMAIL}
      NEXTCLOUD_TRUSTED_DOMAINS: "nextcloud.localhost:${SYSTEM_HTTP_PORT:-8888}"
      REDIS_HOST: nextcloud-redis
      REDIS_HOST_PASSWORD: ${NEXTCLOUD_REDIS_PASSWORD}
      OVERWRITEPROTOCOL: http
    depends_on:
      nextcloud-db:
        condition: service_healthy
      nextcloud-redis:
        condition: service_healthy
    volumes:
      - ./volumes/nextcloud/html:/var/www/html
      - ./volumes/nextcloud/config:/var/www/html/config
      - ./volumes/nextcloud/data:/var/www/html/data
      - ./volumes/nextcloud/custom_apps:/var/www/html/custom_apps
      - ./config/nextcloud/theme:/var/www/html/themes/aiw:ro
      - ./config/nextcloud/no_skeleton.config.php:/tmp/no_skeleton.config.php
      - ./config/nextcloud/sso_block.php:/tmp/sso_block.php
      - ./config/nextcloud/lc_logout_redirect.php:/tmp/lc_logout_redirect.php
      - ./config/nextcloud/session_security_block.php:/tmp/session_security_block.php
      - ./config/nextcloud/pre_install.sh:/docker-entrypoint-hooks.d/pre-installation/pre_install.sh
      - ./config/nextcloud/patch_nextcloud.sh:/docker-entrypoint-hooks.d/before-starting/patch_nextcloud.sh
      - ./config/nextcloud/set_trusted_proxies.sh:/docker-entrypoint-hooks.d/before-starting/set_trusted_proxies.sh
    networks:
      - nocodenation_all_in_wonder_network

  collabora:
    image: collabora/code
    container_name: collabora
    restart: unless-stopped
    cap_add:
      - MKNOD
    environment:
      aliasgroup1: "http://nextcloud.localhost:${SYSTEM_HTTP_PORT:-8888}"
      extra_params: "--o:ssl.enable=false --o:ssl.termination=false --o:mount_jail_tree=false"
      server_name: "nextcloud.localhost:${SYSTEM_HTTP_PORT:-8888}"
      dictionaries: "de_DE en_GB en_US"
      username: "collabora"
      password: "collabora"
      DONT_GEN_SSL_CERT: "true"
    volumes:
      - ./volumes/collabora/fonts:/usr/share/fonts/truetype/custom
      - ./volumes/collabora/fonts:/opt/cool/systemplate/usr/share/fonts/truetype/custom
    networks:
      - nocodenation_all_in_wonder_network

  nifi:
    image: all-in-wonder/nifi:latest
    container_name: nifi
    restart: unless-stopped
    environment:
      SINGLE_USER_CREDENTIALS_USERNAME: ${NIFI_USERNAME}
      SINGLE_USER_CREDENTIALS_PASSWORD: ${NIFI_PASSWORD}
      NIFI_WEB_PROXY_HOST: "nifi.localhost:${SYSTEM_HTTPS_PORT:-8833}"
      NIFI_WEB_HTTPS_PORT: ${SYSTEM_HTTPS_PORT:-8833}
      NIFI_WEB_HTTPS_HOST: "0.0.0.0"
    volumes:
      - ./volumes/data:/data
      - ./volumes/python_extensions:/opt/nifi/nifi-current/python_extensions
      - ./volumes/nar_extensions:/opt/nifi/nifi-current/nar_extensions
      - ./volumes/nifi/conf:/opt/nifi/nifi-current/conf
      - ./volumes/nifi/database_repository:/opt/nifi/nifi-current/database_repository
      - ./volumes/nifi/flowfile_repository:/opt/nifi/nifi-current/flowfile_repository
      - ./volumes/nifi/content_repository:/opt/nifi/nifi-current/content_repository
      - ./volumes/nifi/provenance_repository:/opt/nifi/nifi-current/provenance_repository
      - ./volumes/nifi/state:/opt/nifi/nifi-current/state
      - ./volumes/nginx/certs:/certs:ro
    networks:
      nocodenation_all_in_wonder_network:

  hermes:
#    image: nousresearch/hermes-agent
    image: all-in-wonder/hermes:latest
    container_name: hermes
    restart: unless-stopped
    extra_hosts:
      - "embedding_host:${OPENCODE_EMBEDDING_HOST_IP:-127.0.0.1}"
    ports:
      - "9119:9119"
      - "8642:8642"
      - "8644:8644"
    volumes:
      - ./volumes/hermes:/root/.hermes
      # API keys / secrets: hermes auto-loads ~/.hermes/.env at startup
      # (load_hermes_dotenv, override=true). Bind-mounting the file over the
      # volume makes config/hermes/.env the source of truth — edit it and
      # restart. Copy config/hermes/.env.example to .env and fill in keys.
      - ./config/hermes/.env:/opt/.env
      - ./config/hermes/config.yaml:/opt/config.yaml
#      - ./config/hermes/entrypoint.sh:/entrypoint.sh
      - ./config/agents/skills:/opt/custom-skills
      - ./config/agents/instructions.md:/opt/SOUL.md
      - ./config/hermes/plugins/ingest_pdf:/opt/plugins/ingest_pdf:ro
      - ./volumes/data:/data
    environment:
      API_SERVER_ENABLED: true
      API_SERVER_HOST: 0.0.0.0
      API_SERVER_KEY: ${HERMES_API_KEY}
      API_SERVER_CORS_ORIGINS: '*'
      WEBHOOK_ENABLED: true
      WEBHOOK_PORT: 8644
      WEBHOOK_SECRET: ${HERMES_API_KEY:-}
      HERMES_DASHBOARD: 1
      HERMES_DASHBOARD_TUI: 1
      HERMES_DASHBOARD_INSECURE: 1
      HERMES_DASHBOARD_HOST: 0.0.0.0
      HERMES_DASHBOARD_PORT: 9119
      GATEWAY_ALLOW_ALL_USERS: true
      CUSTOM_SKILLS_FOLDER: /opt/custom-skills
      POSTGREST_API_KEY: ${API_KEY:-}
      POSTGREST_URL: ${POSTGREST_URL:-http://postgrest_app:3000}
      OPENCODE_EMBEDDING_HOST: ${OPENCODE_EMBEDDING_HOST:-}
      OPENCODE_EMBEDDING_MODEL: ${OPENCODE_EMBEDDING_MODEL:-}
      OPENCODE_OPENAI_KEY: ${OPENCODE_OPENAI_KEY:-}
      NIFI_USERNAME: ${NIFI_USERNAME:-}
      NIFI_PASSWORD: ${NIFI_PASSWORD:-}
      SYSTEM_HTTP_PORT: ${SYSTEM_HTTP_PORT:-8888}
      SYSTEM_HTTPS_PORT: ${SYSTEM_HTTPS_PORT:-8833}
      NIFI_KEYSTORE_PASSWORD: ${NIFI_KEYSTORE_PASSWORD}
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
#      HERMES_UID: 0
    command: ["gateway", "run"]
    networks:
      - nocodenation_all_in_wonder_network

  openclaw-gateway:
    image: all-in-wonder/openclaw:latest
    build: .
    # Rootless/userns-remapped Docker maps the host user (uid 1000) to container
    # root (0:0), so the bind-mounted volumes/ (host-owned by 1000) appear as 0:0
    # inside the container. Run as 0:0 so the process maps back to the host UID
    # that owns the mounts; otherwise the image's node user (uid 1000) hits EACCES
    # writing /home/node/.openclaw/state. Container-root is still your unprivileged
    # host user under userns remap (cap_drop / no-new-privileges still apply).
    user: "0:0"
    container_name: openclaw-gateway
    env_file:
      - path: config/openclaw/.env
        required: false
    environment:
      HOME: /home/node
      OPENCLAW_HOME: /home/node
      TERM: xterm-256color
      # Pin container-side state, workspace, and config paths so host values written to
      # `.env` (used by Compose for the bind-mount source below) cannot leak
      # into runtime code that resolves these env vars inside the container.
      # Without this override, a macOS host path like /Users/<you>/.openclaw/...
      # imported from .env caused first-reply `mkdir '/Users'` EACCES failures
      # in Linux Docker (#77436).
      OPENCLAW_STATE_DIR: /home/node/.openclaw
      OPENCLAW_CONFIG_PATH: /home/node/.openclaw/openclaw.json
      OPENCLAW_CONFIG_DIR: /home/node/.openclaw
      OPENCLAW_WORKSPACE_DIR: /home/node/.openclaw/workspace
      # Auth is delegated to the nginx proxy (gateway.auth.mode=trusted-proxy in
      # openclaw.json, patched by config/scripts/start/openclaw.sh). The dashboard
      # no longer uses a gateway token, so it is intentionally not passed here.
      # OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN:-}
      OPENCLAW_ALLOW_INSECURE_PRIVATE_WS: ${OPENCLAW_ALLOW_INSECURE_PRIVATE_WS:-}
      # Empty means auto: Bonjour disables itself in detected containers.
      # Set 0 only on host/macvlan/mDNS-capable networks; set 1 to force off.
      OPENCLAW_DISABLE_BONJOUR: ${OPENCLAW_DISABLE_BONJOUR:-}
      # OpenTelemetry export is outbound OTLP/HTTP from the Gateway. Prometheus
      # uses the existing authenticated Gateway route; it does not need a port.
      OTEL_EXPORTER_OTLP_ENDPOINT: ${OTEL_EXPORTER_OTLP_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_TRACES_ENDPOINT: ${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_METRICS_ENDPOINT: ${OTEL_EXPORTER_OTLP_METRICS_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_LOGS_ENDPOINT: ${OTEL_EXPORTER_OTLP_LOGS_ENDPOINT:-}
      OTEL_EXPORTER_OTLP_PROTOCOL: ${OTEL_EXPORTER_OTLP_PROTOCOL:-http/protobuf}
      OTEL_SERVICE_NAME: ${OTEL_SERVICE_NAME:-}
      OTEL_SEMCONV_STABILITY_OPT_IN: ${OTEL_SEMCONV_STABILITY_OPT_IN:-}
      OPENCLAW_OTEL_PRELOADED: ${OPENCLAW_OTEL_PRELOADED:-}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      POSTGREST_API_KEY: ${API_KEY:-}
      POSTGREST_URL: ${POSTGREST_URL:-http://postgrest_app:3000}
      OPENCODE_EMBEDDING_HOST: ${OPENCODE_EMBEDDING_HOST:-}
      OPENCODE_EMBEDDING_MODEL: ${OPENCODE_EMBEDDING_MODEL:-}
      OPENCODE_OPENAI_KEY: ${OPENCODE_OPENAI_KEY:-}
      NIFI_USERNAME: ${NIFI_USERNAME:-}
      NIFI_PASSWORD: ${NIFI_PASSWORD:-}
      SYSTEM_HTTP_PORT: ${SYSTEM_HTTP_PORT:-8888}
      SYSTEM_HTTPS_PORT: ${SYSTEM_HTTPS_PORT:-8833}
      NIFI_KEYSTORE_PASSWORD: ${NIFI_KEYSTORE_PASSWORD}
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      TZ: ${OPENCLAW_TZ:-UTC}
    volumes:
      - ./volumes/_openclaw:/home/node/.openclaw
      - ./volumes/_openclaw/workspace:/home/node/.openclaw/workspace
      - ./volumes/_openclaw-auth-profile-secrets:/home/node/.config/openclaw
      # Skills: bind config/agents/skills into precedence slot #4
      # (~/.openclaw/skills). No openclaw.json edit needed — OpenClaw scans
      # this path at load time. Read-only: these are config-managed, not written
      # by the agent.
      - ./config/agents/skills:/home/node/.openclaw/skills:ro
      # Plugins: bind the prebuilt plugin tree. Registered via plugins.load.paths
      # in openclaw.json (set by config/scripts/start/openclaw.sh). Each plugin's
      # dist/*.mjs is a self-contained bundle, so no node_modules is needed at
      # runtime. Read-only: the bundle + manifest are built/committed via build.sh.
      - ./config/openclaw/plugins:/home/node/.openclaw/plugins:ro
      ## Uncomment the lines below to enable sandbox isolation
      ## (agents.defaults.sandbox). Requires Docker CLI in the image
      ## (build with --build-arg OPENCLAW_INSTALL_DOCKER_CLI=1) or use
      ## scripts/docker/setup.sh with OPENCLAW_SANDBOX=1 for automated setup.
      ## Set DOCKER_GID to the host's docker group GID (run: stat -c '%g' /var/run/docker.sock).
      # - /var/run/docker.sock:/var/run/docker.sock
    # group_add:
    #   - "${DOCKER_GID:-999}"
    # Let bundled local-model providers reach host-side LM Studio/Ollama via
    # http://host.docker.internal:<port>. Docker Desktop usually provides this
    # alias; the host-gateway mapping makes it work on Linux Docker Engine too.
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    extra_hosts:
      - "host.docker.internal:host-gateway"
      # Map the embedding endpoint hostname the same way the opencode service does
      # (compose.yml ~line 264). OPENCODE_EMBEDDING_HOST is http://embedding_host:8801,
      # but "embedding_host" is not a container — it resolves via this static hosts
      # entry to OPENCODE_EMBEDDING_HOST_IP. Without it the ingest_pdf tool's embed
      # calls fail DNS resolution inside the gateway (HTTP 000 / "host unreachable").
      - "embedding_host:${OPENCODE_EMBEDDING_HOST_IP:-127.0.0.1}"
    # Behind the proxy (like opencode): no host ports are published. The gateway
    # is reached only through nginx via openclaw.localhost (18789),
    # bridge.openclaw.localhost (18790), and msteams.openclaw.localhost (3978).
    # `--bind lan` (below) keeps the gateway listening on the container interface
    # so the proxy — in a different network namespace — can reach it. The
    # openclaw-cli still talks to 127.0.0.1:18789 via the shared netns, unaffected.
    # ports:
    #   - "${OPENCLAW_GATEWAY_PORT:-18789}:18789"
    #   - "${OPENCLAW_BRIDGE_PORT:-18790}:18790"
    #   - "${OPENCLAW_MSTEAMS_PORT:-3978}:3978"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        # "loopback" binds 127.0.0.1 only, so the published port (above) never
        # forwards and other containers can't reach it. "lan" binds the
        # container's network interface, which is what the Docker port mapping
        # and inter-service traffic need. Valid values: loopback|lan|tailnet|
        # auto|custom (the previous "local" is not valid and crash-loops with
        # "Invalid --bind").
        "--bind",
        "lan",
        "--port",
        "18789",
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks:
      nocodenation_all_in_wonder_network:

  openclaw-cli:
    image: all-in-wonder/openclaw:latest
    network_mode: "service:openclaw-gateway"
    # See openclaw-gateway above: run as 0:0 so the rootless/userns-remapped host
    # UID owns the bind-mounted volumes/ and avoids EACCES on writes.
    user: "0:0"
    container_name: openclaw-cli
    env_file:
      - path: config/openclaw/.env
        required: false
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    # No extra_hosts here: Docker rejects custom host-to-IP mappings combined with
    # network_mode: service: (below). The CLI shares the gateway's network namespace,
    # and the ingest_pdf embed calls run in the gateway process anyway, so the
    # embedding_host mapping on openclaw-gateway is what matters.
    environment:
      HOME: /home/node
      OPENCLAW_HOME: /home/node
      TERM: xterm-256color
      # Pin container-side state, workspace, and config paths so host values written to
      # `.env` cannot leak into runtime code via the env_file import (#77436).
      OPENCLAW_STATE_DIR: /home/node/.openclaw
      OPENCLAW_CONFIG_PATH: /home/node/.openclaw/openclaw.json
      OPENCLAW_CONFIG_DIR: /home/node/.openclaw
      OPENCLAW_WORKSPACE_DIR: /home/node/.openclaw/workspace
      # Auth is delegated to the nginx proxy (gateway.auth.mode=trusted-proxy in
      # openclaw.json, patched by config/scripts/start/openclaw.sh). The dashboard
      # no longer uses a gateway token, so it is intentionally not passed here.
      # OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN:-}
      OPENCLAW_ALLOW_INSECURE_PRIVATE_WS: ${OPENCLAW_ALLOW_INSECURE_PRIVATE_WS:-}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      TZ: ${OPENCLAW_TZ:-UTC}
      POSTGREST_API_KEY: ${API_KEY:-}
      POSTGREST_URL: ${POSTGREST_URL:-http://postgrest_app:3000}
      OPENCODE_EMBEDDING_HOST: ${OPENCODE_EMBEDDING_HOST:-}
      OPENCODE_EMBEDDING_MODEL: ${OPENCODE_EMBEDDING_MODEL:-}
      OPENCODE_OPENAI_KEY: ${OPENCODE_OPENAI_KEY:-}
      NIFI_USERNAME: ${NIFI_USERNAME:-}
      NIFI_PASSWORD: ${NIFI_PASSWORD:-}
      SYSTEM_HTTP_PORT: ${SYSTEM_HTTP_PORT:-8888}
      SYSTEM_HTTPS_PORT: ${SYSTEM_HTTPS_PORT:-8833}
      NIFI_KEYSTORE_PASSWORD: ${NIFI_KEYSTORE_PASSWORD}
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
    volumes:
      - ./volumes/_openclaw:/home/node/.openclaw
      - ./volumes/_openclaw/workspace:/home/node/.openclaw/workspace
      - ./volumes/_openclaw-auth-profile-secrets:/home/node/.config/openclaw
      # Skills: same mount as openclaw-gateway so the CLI (and `setup`) see the
      # same skill set. See the gateway service for details.
      - ./config/agents/skills:/home/node/.openclaw/skills:ro
      # Plugins: same mount as openclaw-gateway. See the gateway service for details.
      - ./config/openclaw/plugins:/home/node/.openclaw/plugins:ro
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - openclaw-gateway
    # No `networks:` here: `network_mode: service:openclaw-gateway` (above) makes
    # this container share the gateway's network namespace, which is already
    # attached to nocodenation_all_in_wonder_network. Declaring both is a Compose
    # error ("mutually exclusive network_mode and networks").

networks:
  nocodenation_all_in_wonder_network:
    name: nocodenation_all_in_wonder_network_${SYSTEM_HTTP_PORT:-8888}
